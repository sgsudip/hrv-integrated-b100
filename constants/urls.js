export const url = (text) => {
  // return `https://lubdub-testing.herokuapp.com/${text}`   //test server
  // return `http://192.168.1.70:4000/${text}`; //local server
  return `https://app.b100quiz.com/${text}`;
  //  return `https://b100-backend.herokuapp.com/${text}`; //production server
};
